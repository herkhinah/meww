(fun hello () (print "hello world"))

(fun plus (a b) (+ a b))

(data Maybe ('a)
    (Nothing)
    (Just 'a))

(data Tree ('a)
    (Node (Leaf 'a) (Leaf 'a))
    (Leaf 'a))

(fun mapJust (a, map)
    (case a
        ((Just a) (Just (map a)))
        (Nothing Nothing)))